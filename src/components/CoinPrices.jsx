import API from "./API";

const CoinPrices = () => {
    const getCoinPrices = () => {};

    return (
        <div>
            <h2>Coin Prices</h2>
            <API />
        </div>
    );
};

export default CoinPrices;
